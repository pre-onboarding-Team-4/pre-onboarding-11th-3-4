import React from 'react';
import styled from 'styled-components';

const Loading = () => {
  return <Root>로딩중입니다...</Root>;
};

const Root = styled.div`
  text-align: center;
`;

export default Loading;
